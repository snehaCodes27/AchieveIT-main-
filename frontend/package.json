{
  "name": "achievement-management-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.4"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.56.0",
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^7.3.6",
    "wrangler": "^4.135.0"
  }
}
